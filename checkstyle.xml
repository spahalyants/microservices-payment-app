<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">

    <!-- Set file encoding -->
    <property name="charset" value="UTF-8"/>

    <!-- Check maximum line length (file-level check, must be under Checker) -->
    <module name="LineLength">
        <property name="max" value="120"/>
    </module>

    <!-- Analyze Java source code using AST -->
    <module name="TreeWalker">

        <!-- Require whitespace after certain tokens (commas, semicolons, etc.) -->
        <module name="WhitespaceAfter"/>

        <!-- Require whitespace around operators -->
        <module name="WhitespaceAround"/>

        <!-- Enforce 'final' for local variables that are not reassigned -->
        <module name="FinalLocalVariable"/>

        <!-- Detect and forbid unused imports -->
        <module name="UnusedImports"/>

        <!-- Enforce indentation rules -->
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
            <property name="braceAdjustment" value="0"/>
            <property name="caseIndent" value="4"/>
            <property name="throwsIndent" value="4"/>
            <property name="arrayInitIndent" value="4"/>
            <property name="lineWrappingIndentation" value="4"/>
            <property name="forceStrictCondition" value="true"/>
        </module>

    </module>

</module>